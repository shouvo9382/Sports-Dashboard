# -------------------------------------------------------------------------
# EXAMPLE secrets file.  Copy this to  .streamlit/secrets.toml  and set a
# real code to password-protect the dashboard.
#
#   1. In this folder:  copy  secrets.toml.example  ->  .streamlit/secrets.toml
#   2. Change the value below to your own access code.
#
# The real .streamlit/secrets.toml is git-ignored and must NEVER be committed.
# On Streamlit Community Cloud you do NOT upload this file — instead you paste
# the same line into the app's  Settings ▸ Secrets  panel.
#
# If no app_password is set anywhere, the dashboard runs WITHOUT a login
# (fine for offline/local use via run_dashboard.bat).
# -------------------------------------------------------------------------

app_password = "YourMinistryCode2026"
